<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fatec.com.br.appprofangela">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

<!--    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> -->

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrajetosDoDia"
            android:parentActivityName=".CaronaDoDia">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CaronaDoDia" />
        </activity>
        <activity android:name=".DiaSemanaSelecionado"
            android:parentActivityName=".DiasSemana">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DiasSemana" />
        </activity>
        <activity
            android:name=".Recorrencia"
            android:theme="@style/Theme.CaixaDialogo"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".Trajetos">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Trajetos" />
        </activity>
        <activity
            android:name=".CaixaDialogo"
            android:theme="@style/Theme.CaixaDialogo"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".Trajetos">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Trajetos" />
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps" />
        <activity android:name=".DiasSemana"
            android:parentActivityName=".GrupoCarona">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GrupoCarona" />
        </activity>
        <activity android:name=".Carregamento">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:screenOrientation="portrait"
            android:parentActivityName=".SplashCarregamento">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SplashCarregamento" />
        </activity>
        <activity
            android:name=".EsqueciSenha"
            android:screenOrientation="portrait"
            android:parentActivityName=".Login">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Login" />
        </activity>
        <activity
            android:name=".Cadastro"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"
            android:parentActivityName=".Login">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Login" />
        </activity>
        <activity
            android:name=".SplashCarregamento"
            android:screenOrientation="portrait" />
        <activity
            android:name=".NovoGrupo"
            android:screenOrientation="portrait"
            android:parentActivityName=".GrupoCarona">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GrupoCarona" />
        </activity>
        <activity
            android:name=".DiaAtual"
            android:screenOrientation="portrait"
            android:parentActivityName=".GrupoCarona"
            android:windowSoftInputMode="stateHidden">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GrupoCarona" />
        </activity>
        <activity
            android:name=".CaronaDoDia"
            android:screenOrientation="portrait"
            android:parentActivityName=".DiaAtual">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DiaAtual" />
        </activity>
        <activity
            android:name=".GrupoCarona"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:parentActivityName=".GrupoCarona">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GrupoCarona" />
        </activity>
        <activity
            android:name=".Trajetos"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"
            android:parentActivityName=".DiasSemana">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DiasSemana" />
        </activity>
        <activity android:name=".AdicionaCaroneiro"
            android:parentActivityName=".DiaAtual">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DiaAtual" />
        </activity>
        <activity android:name=".CaroneiroIdaVolta"
            android:parentActivityName=".AdicionaCaroneiro">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdicionaCaroneiro" />
        </activity>
        <activity android:name=".RelatorioDias"
            android:parentActivityName=".DiaAtual">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DiaAtual" />
        </activity>
    </application>

</manifest>